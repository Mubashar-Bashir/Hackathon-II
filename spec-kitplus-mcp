#!/bin/bash
# Wrapper script for Spec-Kit Plus MCP server

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the Python server
exec python3 "$SCRIPT_DIR/mcp/servers/spec-kit-plus/server.py"